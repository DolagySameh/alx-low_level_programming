#include <stdio.h>
#include "main.h"
/**
* _abs - print
* Description:print
* @a:first age
* Return:intvalue
*/
int _abs(int a)
{
	if (a < 0)
	{
		a = (-1) * a;
		return (a);
	}
	else
	{
		return (a);
	}
}
